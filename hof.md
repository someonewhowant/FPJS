# Модуль 4: Функции высшего порядка (HOF)

Функция, которая принимает другие функции как аргументы или возвращает их.

## 1. Примеры
- Методы массивов: `map`, `filter`, `reduce`.
- Декораторы: `withLogger`, `memoize`.
